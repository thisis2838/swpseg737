@model dynamic

<div class="account-card">
    <div class="account-card-header">
        <i class="fa fa-@(Model.Icon)"></i>
        <span>@Model.Header</span>
    </div>
    <div class="account-card-content">@Model.Content</div>
</div>
