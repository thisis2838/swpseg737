@model dynamic

<div class="card">
    <div class="card-body">
        <div class="row">
            <div class="col mt-0">
                <h5 class="card-title">@Model.Heading</h5>
            </div>

            <div class="col-auto">
                <div class="stat text-primary">
                    <i class="align-middle" data-feather="@Model.Icon"></i>
                </div>
            </div>
        </div>
        <h1 class="mt-1 mb-3">@Model.Value</h1>
        @*
        <div class="mb-0">
            <span class="badge badge-success-light"> <i class="mdi mdi-arrow-bottom-right"></i> TODO%</span>
            <span class="text-muted">Since last week</span>
        </div>
        *@
    </div>
</div>