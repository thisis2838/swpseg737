@model HoaLacLaptopShop.Models.LaptopCPUSeries
@{
    ViewData["Title"] = "Update";
}

<partial 
    name="~/Areas/Administration/Views/Shared/_PageHeading.cshtml" 
    model="@(new{ Heading = "Component Series", Subheading = $"CPU #{Model.ID}"})" 
/>

<div>
    <div value="Save" onclick="$('#main-form').submit();" class="btn btn-primary">Save</div>
    <div class="btn btn-danger" onclick="confirmDelete()">Delete</div>
    <a class="btn btn-primary" asp-action="Index">Back to List</a>
</div>
<hr />

<div class="col-md-4">
    <form asp-action="Edit" id="main-form">
        <partial name="_CPUInfoView.cshtml" model="@Model" />
    </form>
</div>

<form asp-action="Delete" id="form-delete">
    <input type="hidden" asp-for="ID" />
</form>

@section Scripts
{
    <script>
        function confirmDelete()
        {
            if (confirm('Are you sure you want to delete this CPU Series?'))
            {
                $('#form-delete').submit();
            }
        }
    </script>
}